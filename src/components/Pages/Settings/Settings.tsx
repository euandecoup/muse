import React from "react";
import styles from "./Settings.module.css";

const Settings: React.FC = () => (
  <div className={styles.pageContainer}>
    <h1>Settings</h1>
    {/* Settings content TBC */}
  </div>
);

export default Settings;
